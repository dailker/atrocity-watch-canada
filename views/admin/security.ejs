<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Security Settings - Atrocity Watch Canada</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background: #121212; color: #f5f5f7; font-family: 'Roboto', sans-serif; }
        .matrix-font { font-family: 'Orbitron', sans-serif; }
    </style>
</head>
<body>
<%- include('../partials/adminHeader', { user: user }) %>
<div class="flex items-center justify-center min-h-screen pt-24">
    <div class="bg-black bg-opacity-80 p-8 rounded-lg shadow-lg w-full max-w-md border border-green-400/30">
        <h1 class="text-2xl font-bold matrix-font text-center mb-6 text-green-400">Security Settings</h1>
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="mb-4 text-red-500 text-center"><%= error %></div>
        <% } %>
        <% if (typeof success !== 'undefined' && success) { %>
            <div class="mb-4 text-green-500 text-center"><%= success %></div>
        <% } %>
        <form method="POST" action="/admin/security" class="space-y-6">
            <div>
                <label class="block mb-1 matrix-font" for="currentPassword">Current Password</label>
                <input class="w-full px-4 py-2 rounded bg-gray-900 border border-green-400/30 focus:outline-none focus:border-green-400" type="password" id="currentPassword" name="currentPassword" required>
            </div>
            <div>
                <label class="block mb-1 matrix-font" for="newPassword">New Password</label>
                <input class="w-full px-4 py-2 rounded bg-gray-900 border border-green-400/30 focus:outline-none focus:border-green-400" type="password" id="newPassword" name="newPassword" required>
            </div>
            <div>
                <label class="block mb-1 matrix-font" for="confirmPassword">Confirm New Password</label>
                <input class="w-full px-4 py-2 rounded bg-gray-900 border border-green-400/30 focus:outline-none focus:border-green-400" type="password" id="confirmPassword" name="confirmPassword" required>
            </div>
            <button class="w-full btn-primary px-4 py-2 rounded matrix-font font-bold" type="submit">Change Password</button>
        </form>
    </div>
</div>
<%- include('../partials/adminFooter') %>
</body>
</html>
