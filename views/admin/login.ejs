<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Admin Login - Atrocity Watch Canada</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background: #121212;
            color: #f5f5f7;
            font-family: 'Roboto', sans-serif;
        }
        .matrix-font {
            font-family: 'Orbitron', sans-serif;
        }
    </style>
</head>
<body class="flex items-center justify-center min-h-screen pt-32">
    <div class="bg-black bg-opacity-80 p-8 rounded-lg shadow-lg w-full max-w-md border border-green-400/30">
        <h1 class="text-2xl font-bold matrix-font text-center mb-6 text-green-400">Admin Login</h1>
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="mb-4 text-red-500 text-center"><%= error %></div>
        <% } %>
        <form method="POST" action="/admin/login" class="space-y-6">
            <div>
                <label class="block mb-1 matrix-font" for="username">Username</label>
                <input class="w-full px-4 py-2 rounded bg-gray-900 border border-green-400/30 focus:outline-none focus:border-green-400" type="text" id="username" name="username" required>
            </div>
            <div>
                <label class="block mb-1 matrix-font" for="password">Password</label>
                <input class="w-full px-4 py-2 rounded bg-gray-900 border border-green-400/30 focus:outline-none focus:border-green-400" type="password" id="password" name="password" required>
            </div>
            <button class="w-full btn-primary px-4 py-2 rounded matrix-font font-bold" type="submit">Login</button>
        </form>
    </div>
</body>
</html>
